[core]
	excludesfile = /Users/lhn/.gitignore
[user]
    name = "Lorrin Nelson"
	email = lhn@mult.ifario.us
# Merge config from http://www.andymcintosh.com/?p=33
[merge]
    keepBackup = false;
    tool = p4merge

#This is no longer needed. Git has built-in support for p4merge now
#[mergetool "p4merge"]
#    cmd = p4merge "$BASE" "$LOCAL" "$REMOTE" "$MERGED"
#    keepTemporaries = false
#    trustExitCode = false
#    keepBackup = false
# Also don't need anything for XCode's FileMerge.app -- just use -t opendiff

# Leave git diff alone because git diff | vim - is quick-n-easy vs. scrolling
# around one file at a time in a GUI.
# Can always use git difftool to launch p4merge
#[diff]
#    external = p4diff
